{
    "name": "travel-app",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
      "client",
      "api"
    ],
   "scripts": {
    "build": "cd client && yarn install && yarn build",
    "start": "yarn workspace api start"
   }
}