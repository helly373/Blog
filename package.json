{
    "name": "travel-app",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
      "client",
      "api"
    ],
    "engines": {"node": "20.x"},
   "scripts": {
    "build": "cd client && yarn install && yarn build",
    "start": "yarn workspace api start"
   }
}